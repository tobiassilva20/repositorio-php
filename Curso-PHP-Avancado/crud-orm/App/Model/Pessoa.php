<?php

namespace App\Model;
use ActiveRecord\Model;

/**
 * Classe Pessoa que corresponderá a uma tabela de mesmo * nome gerada no banco
 * de dados pelo orm active record.
 */
class Pessoa extends Model{
    // Não foi necessário o uso de atributos.
}